# Production runtime requirements for the hosted FastAPI backend.
# Strictly what backend/ + shared/ import at runtime — drops training-only
# deps (peft, bitsandbytes, accelerate, trl, datasets, sklearn, evaluate,
# bert-score, nltk, wandb) that bloat the image without being used.

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Image processing
pillow>=10.3.0
pillow-heif>=0.18.0  # decode iPhone HEIC/HEIF uploads (registers a Pillow plugin)

# Schemas
pydantic>=2.7.0

# Database
aiosqlite>=0.20.0

# HTTP clients
httpx>=0.27.0
openai>=1.55.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.0.0

# ML — DINOv2 + the 3 head MLPs are loaded at startup. The VLM (Qwen3-VL)
# itself runs on RunPod Serverless, but the heads run locally.
# torch is installed separately in the Dockerfile from the CPU-only index
# to avoid pulling ~1.5 GB of CUDA libs we can't use on this t2.small.
transformers>=4.56,<5
numpy>=1.26.0
huggingface_hub>=0.20.0
# pandas + scikit-learn + tqdm are imported at module load by the head
# definition files (models/train_*.py), even though the training-only
# functions inside them aren't called at inference time. Cheaper to ship
# the deps than to refactor the head modules.
pandas>=2.2.0
scikit-learn>=1.5.0
tqdm>=4.66.0
