# Pinned to the versions we verified during training extraction (Day-3 RunPod
# session). transformers >= 4.56 is the floor for Qwen3-VL.
transformers>=4.56,<5
peft>=0.11.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
huggingface_hub>=0.36.0
runpod>=1.6.0
pillow>=10.3.0
